import re
import email.utils

pattern = r'^[a-zA-Z][\w\.-]*@[a-zA-Z]+\.[a-zA-Z]{1,3}$'

n = int(input())
for _ in range(n):
    s = input().strip()
    name, addr = email.utils.parseaddr(s)
    if re.match(pattern, addr):
        print(s)
